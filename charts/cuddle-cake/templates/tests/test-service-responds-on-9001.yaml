apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-200-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}-credentials-test
    image: byrnedo/alpine-curl:latest
    command: ["-vvv","cuddle-cake:9001"]
  restartPolicy: Never
