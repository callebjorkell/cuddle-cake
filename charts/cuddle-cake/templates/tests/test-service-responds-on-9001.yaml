apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-{{ randAlpha 8 }} -200-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}-credentials-test
    image: byrnedo/alpine-curl:latest
    command: ["/bin/true"]
  restartPolicy: Never
